<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
</head>
<body>
<% include header.ejs %>
<br>
<!-- Begin page content -->
<div class="container" style="margin-top: 50px">
    <table class="table table-hover">
        <tr>
            <th>#</th>
            <th>보호자 전화번호</th>
            <th>사용자 전화번호</th>
            <th>발신 위치</th>
            <th>발신 시각</th>
            <th>녹음파일</th>
            <th>차량 번호</th>
        </tr>

        <% if (data) {
        for (var i = 0; i < data.length; i++) {
            var each = data[i]; %>
        <tr>
            <td><%= each.id %></td>
            <td><%= each.receiver %></td>
            <td><%= each.caller %></td>
            <% if (each.location) { %>
            <td><a href="/main/location/<%= each.id %>">확인</a></td>
            <% } else { %>
            <td>없음</td>
            <% } %>

            <td><%= each.time %></td>
            <% if (each.file) { %>
            <td>
                <audio controls="controls">
                    <source src="<%= each.file %>" type="audio/mp3"/>
                    <source src="<%= each.file %>" type="audio/ogg"/>
                    <p>지원하지 않는 브라우저 입니다!</p>
                </audio>
            </td>
            <% }
            else { %>
            <td>없음</td>
            <% } %>

            <td><%= each.car_number %></td>
        </tr>
        <% }
        } %>

    </table>
</div>

</body>
</html>